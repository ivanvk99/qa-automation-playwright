# Claude Command Notes

The following types of instructions were used with Claude Code:

- generate a Playwright test suite for the photo gallery application
- focus on homepage, gallery, search, login, upload, and album flows
- avoid using the internet or external repositories
- work only with local project files
- refine only failing tests
- preserve existing passing tests
- improve selectors, waits, and assertions
- reduce flaky behavior without over-reducing coverage